/*import {operators} from "../operators";
import {app} from "../main";
import assert from 'assert';

describe('Calculator operations:', () => {

  it('Subtraction test:', () => {
    let operation = new operators(document.createElement('div'));
    operation.firstNumber = 4;
    operation.secondNumber = 2;
    operation.subtraction();
    assert.equal(operation.board.innerHTML, 2);
  });

  it('Multiplication test:', () => {
    let operation = new operators(document.createElement('div'));
    operation.firstNumber = 3;
    operation.secondNumber = 2;
    operation.multiplication();
    assert.equal(operation.board.innerHTML, 6);
  });

  it('Division test:', () => {
    let operation = new operators(document.createElement('div'));
    operation.firstNumber = 12;
    operation.secondNumber = 3;
    operation.division();
    assert.equal(operation.board.innerHTML, 4);
  });

});*/
